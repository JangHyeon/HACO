<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 구현하고자하는 인터페이스 풀경로 -->
<mapper namespace="kr.co.haco.DAO.AccountDAO">
	<!-- id명은 인터페이스가 구현하는 메서드명 -->
	<!-- resultType은 랩퍼클래스 타입 -->



	<select id="getRoleList" resultType="kr.co.haco.VO.Roles">
		select role_name, role_desc,
		create_date from roles
	</select>

	<select id="getEmployee" parameterType="Integer" resultType="kr.co.haco.VO.Employee">
		select * from employee where account_id=#{param1}
	</select>

	<select id="getEmployeeID" parameterType="Integer"
		resultType="kr.co.haco.VO.Employee">
		select account_id from employee where name_kor like
		#{param1}
	</select>


	<!-- <select id="getCourseList" resultType="kr.co.haco.VO.OpenCourse"> select 
		open_course_id,account_id,subject_id,course_name,classroom, course_start_date,course_end_date,center_id 
		from open_course </select> -->

	
</mapper>